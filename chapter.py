# Module : A module is a flie containing code written by somebody else(usually) which can be imported and used in our progarms.for example modules like flask,django,etc
# PIP : Pip is the package manager for python.You can use pip to install a module on your system.
# For example
import pyjokes
joke = pyjokes.get_joke()
print(joke)
